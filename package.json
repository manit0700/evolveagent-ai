{
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"cd backend && ./venv/bin/uvicorn app.main:app --reload --port 8001\" \"cd frontend && npm run dev\"",
    "test": "cd backend && ./venv/bin/pytest",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
