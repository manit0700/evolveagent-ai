# Linear handoff — EVO-174

**Title:** Allowlisted test/build runner
**Branch:** `linear/evo-174`
**Linear:** https://linear.app/ai-stock-analysis-portfolio/issue/EVO-174/allowlisted-testbuild-runner

## Description
pytest and npm run build only

## Mission Control goal
- Goal: Allowlisted test/build runner
- Goal ID: `ef247180-0b77-4479-b4d7-a6ebd4a9eb45`

## Subtasks
- [pending] Allowlisted test/build runner

## Checkout
```bash
git fetch origin
git checkout linear/evo-174
```

## Cursor Agent prompt
```markdown
You are working on Linear issue **EVO-174**: Allowlisted test/build runner

## Task
Allowlisted test/build runner

pytest and npm run build only

## Repo rules
- Work on branch `linear/evo-174` (checkout if needed)
- Match existing EvolveAgent AI conventions (FastAPI backend + React frontend)
- Do NOT edit `.env`, `node_modules/`, `venv/`, `backend/app/data/`, `backend/app/uploads/`, `__pycache__/`
- Run verification before finishing:
  - `cd backend && ./venv/bin/pytest`
  - `cd frontend && npm run build`
- Commit with message: `Linear EVO-174: <short summary>`

## When done
Summarize what changed, why, and how it was tested. Then use EvolveAgent **Verify Cursor work** or Mission Control **Mark done** to close Linear.
```

## Codex prompt
```text
Implement Linear EVO-174 — Allowlisted test/build runner

Task: Allowlisted test/build runner
pytest and npm run build only

Checkout branch: git checkout linear/evo-174
Project: EvolveAgent AI (FastAPI + React).
Avoid secrets, .env, node_modules, venv, and backend/app/data/.
Verify with pytest and npm run build before completing.
```

## Verify locally
```bash
cd backend && ./venv/bin/pytest
cd frontend && npm run build
```

## Close the loop
- EvolveAgent UI → Linear panel → **Verify Cursor work**
- Or Mission Control → **Mark done** (auto-closes Linear with summary)
