# Linear handoff — EVO-171

**Title:** Git automation service
**Branch:** `linear/evo-171`
**Linear:** https://linear.app/ai-stock-analysis-portfolio/issue/EVO-171/git-automation-service

## Description
Safe branch, commit, optional push

## Mission Control goal
- Goal: Git automation service
- Goal ID: `b18362f2-a0da-46b4-8f1c-b6bd3f885c77`

## Subtasks
- [done] Git automation service

## Checkout
```bash
git fetch origin
git checkout linear/evo-171
```

## Cursor Agent prompt
```markdown
You are working on Linear issue **EVO-171**: Git automation service

## Task
Git automation service

Safe branch, commit, optional push

## Repo rules
- Work on branch `linear/evo-171` (checkout if needed)
- Match existing EvolveAgent AI conventions (FastAPI backend + React frontend)
- Do NOT edit `.env`, `node_modules/`, `venv/`, `backend/app/data/`, `backend/app/uploads/`, `__pycache__/`
- Run verification before finishing:
  - `cd backend && ./venv/bin/pytest`
  - `cd frontend && npm run build`
- Commit with message: `Linear EVO-171: <short summary>`

## When done
Summarize what changed, why, and how it was tested. Then use EvolveAgent **Verify Cursor work** or Mission Control **Mark done** to close Linear.
```

## Codex prompt
```text
Implement Linear EVO-171 — Git automation service

Task: Git automation service
Safe branch, commit, optional push

Checkout branch: git checkout linear/evo-171
Project: EvolveAgent AI (FastAPI + React).
Avoid secrets, .env, node_modules, venv, and backend/app/data/.
Verify with pytest and npm run build before completing.
```

## Verify locally
```bash
cd backend && ./venv/bin/pytest
cd frontend && npm run build
```

## Close the loop
- EvolveAgent UI → Linear panel → **Verify Cursor work**
- Or Mission Control → **Mark done** (auto-closes Linear with summary)
