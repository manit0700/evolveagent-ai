# Linear handoff — EVO-196

**Title:** Memory consolidation job
**Branch:** `linear/evo-196`
**Linear:** https://linear.app/ai-stock-analysis-portfolio/issue/EVO-196/memory-consolidation-job

## Description
Merge duplicate/overlapping memories

## Mission Control goal
- Goal: Memory consolidation job
- Goal ID: `853203a3-cb7e-4c25-9065-9a494b00590a`

## Subtasks
- [pending] Memory consolidation job

## Checkout
```bash
git fetch origin
git checkout linear/evo-196
```

## Cursor Agent prompt
```markdown
You are working on Linear issue **EVO-196**: Memory consolidation job

## Task
Memory consolidation job

Merge duplicate/overlapping memories

## Repo rules
- Work on branch `linear/evo-196` (checkout if needed)
- Match existing EvolveAgent AI conventions (FastAPI backend + React frontend)
- Do NOT edit `.env`, `node_modules/`, `venv/`, `backend/app/data/`, `backend/app/uploads/`, `__pycache__/`
- Run verification before finishing:
  - `cd backend && ./venv/bin/pytest`
  - `cd frontend && npm run build`
- Commit with message: `Linear EVO-196: <short summary>`

## When done
Summarize what changed, why, and how it was tested. Then use EvolveAgent **Verify Cursor work** or Mission Control **Mark done** to close Linear.
```

## Codex prompt
```text
Implement Linear EVO-196 — Memory consolidation job

Task: Memory consolidation job
Merge duplicate/overlapping memories

Checkout branch: git checkout linear/evo-196
Project: EvolveAgent AI (FastAPI + React).
Avoid secrets, .env, node_modules, venv, and backend/app/data/.
Verify with pytest and npm run build before completing.
```

## Verify locally
```bash
cd backend && ./venv/bin/pytest
cd frontend && npm run build
```

## Close the loop
- EvolveAgent UI → Linear panel → **Verify Cursor work**
- Or Mission Control → **Mark done** (auto-closes Linear with summary)
