# Linear handoff — EVO-172

**Title:** Safe file editor apply
**Branch:** `linear/evo-172`
**Linear:** https://linear.app/ai-stock-analysis-portfolio/issue/EVO-172/safe-file-editor-apply

## Description
Approval-gated file patches

## Mission Control goal
- Goal: Safe file editor apply
- Goal ID: `e520e50c-0723-4dcb-a572-c95f86f53ac0`

## Subtasks
- [done] Safe file editor apply

## Checkout
```bash
git fetch origin
git checkout linear/evo-172
```

## Cursor Agent prompt
```markdown
You are working on Linear issue **EVO-172**: Safe file editor apply

## Task
Safe file editor apply

Approval-gated file patches

## Repo rules
- Work on branch `linear/evo-172` (checkout if needed)
- Match existing EvolveAgent AI conventions (FastAPI backend + React frontend)
- Do NOT edit `.env`, `node_modules/`, `venv/`, `backend/app/data/`, `backend/app/uploads/`, `__pycache__/`
- Run verification before finishing:
  - `cd backend && ./venv/bin/pytest`
  - `cd frontend && npm run build`
- Commit with message: `Linear EVO-172: <short summary>`

## When done
Summarize what changed, why, and how it was tested. Then use EvolveAgent **Verify Cursor work** or Mission Control **Mark done** to close Linear.
```

## Codex prompt
```text
Implement Linear EVO-172 — Safe file editor apply

Task: Safe file editor apply
Approval-gated file patches

Checkout branch: git checkout linear/evo-172
Project: EvolveAgent AI (FastAPI + React).
Avoid secrets, .env, node_modules, venv, and backend/app/data/.
Verify with pytest and npm run build before completing.
```

## Verify locally
```bash
cd backend && ./venv/bin/pytest
cd frontend && npm run build
```

## Close the loop
- EvolveAgent UI → Linear panel → **Verify Cursor work**
- Or Mission Control → **Mark done** (auto-closes Linear with summary)
